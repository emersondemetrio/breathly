// Define the breathing steps as an enum
export enum BreathingStep {
  IDLE = "idle",
  PREPARING = "preparing",
  INHALE = "inhale",
  EXHALE = "exhale",
  FINISHED = "finished"
}

export type Language = "pt" | "en";
